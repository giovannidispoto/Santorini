#GitIgnore for repository root :ing-sw-2020-Crippa-Dispoto-Facchinetti

#----------- BLACKLIST -----------
#N.B: Blacklist don't needed in other sub-gitignore
# Ignore everything	(rules = 1)
*

#----------- WHITELIST -----------
#N.B: Other gitignore must implement only the whitelist
# Don't Ignore those folders and files

#root	(rules = 3)
!.gitignore
!pom.xml
!README.md

#ASSETS/ (rules = 2) (You can do everything here, this folder has it's own gitignore)
!ASSETS/
!ASSETS/*

#DELIVERABLES/	(rules = 2) (You can do everything here, this folder has it's own gitignore)
!DELIVERABLES/
!DELIVERABLES/*

#Windows10/     (rules = 2) (You can do everything here, this folder has it's own gitignore)
!windows10/
!windows10/*

#src/	(rules = 7)
!src/
!src/*.json
!src/networkCommunications/
!src/networkCommunications/*.txt
!src/networkCommunications/netJson/
!src/networkCommunications/netJson/msgtoClient/
!src/networkCommunications/netJson/msgtoClient/*.json
!src/networkCommunications/netJson/msgtoClient/*.txt
!src/networkCommunications/netJson/msgToServer/
!src/networkCommunications/netJson/msgToServer/*.json
!src/networkCommunications/netJson/msgToServer/*.txt

#src/main/	(rules = 2) (this folder has it's own gitignore)
!src/main/
!src/main/*

#src/test	(rules = 2) (this folder has it's own gitignore)
!src/test/
!src/test/*

#Maintained by Gorlenah