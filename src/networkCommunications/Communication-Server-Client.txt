	CLIENT														SERVER

-------------------------------------------------------------------------------------- MATCH CREATION
(Do)
	addPlayerRequest		--------------------------------->	ok					{"action":"addPlayer","data":{"playerNickname":"Steve Jobs","lobby":2}}
	Added/NickNameExist		<---------------------------------	Response			(Server check nick presence in every lobby)
(While nick invalid)
	
	LobbyReady				<---------------------------------	lobbyReady			(Server start match when a Lobby is Full, notify all client in Lobby by nick)
	ok						<---------------------------------	godChooseCards		("God"Player whose nickname will be in the message, choose 3 or 2 cards according to the lobby size)
	godChooseCardsResponse	--------------------------------->	ok					(Set Deck, Then send choose card, SelectedPlayer is the last)
	
(For players)
	ok						<---------------------------------	playerChooseCard	(Player chose 1 card, send deck(little))
	playerChooseCard		--------------------------------->	ok					(Server add Player in match, Add card to forbidden cards, Add Color to Player)
	getWorkersID			--------------------------------->	ok
    WorkersID				<---------------------------------	Response
(For Workers)
	getBattlefield			--------------------------------->	ok
	Battlefield				<---------------------------------	Response
	setInitialWorkerPosition--------------------------------->	ok
(end)
	Battlefield				<---------------------------------	setBattlefield		(notify all)
	Players					<---------------------------------	setPlayers			(notify all (nick+color+card))
(end)

-------------------------------------------------------------------------------------- START MATCH
(For Players)
	ok						<---------------------------------	yourTurn
	startTurn				--------------------------------->	ok
(For Steps)
	ok	(check)				<---------------------------------	playStep
(Do)
	selectWorker	(check)	--------------------------------->	ok
	getWorkerView           --------------------------------->	ok
	WorkerView				<---------------------------------	Response (!!!WorkerView NULL no step)
(While WorkerView NULL)
	skipStepRequest(opt)	--------------------------------->	ok
	playStepRequest(opt)	--------------------------------->	ok
	Battlefield				<---------------------------------	setBattlefield		(notify all)
(end)


