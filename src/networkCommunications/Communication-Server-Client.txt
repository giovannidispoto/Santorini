	CLIENT														SERVER

-------------------------------------------------------------------------------------- MATCH CREATION
(Do)
	addPlayerRequest		--------------------------------->	ok					{"action":"addPlayer","data":{"playerNickname":"Steve Jobs","lobbySize":2}}
	check           		<---------------------------------	Response			(Server check Nick presence & lobby Decision)
(While Nick or lobbyState invalid)
	
	LobbyReady				<---------------------------------	lobbyReady			(Server start match when a Lobby is Full, notify all client in Lobby by nick)
	ok						<---------------------------------	setPickedCards		("God"Player whose nickname will be in the message, choose 3 or 2 cards according to the lobby size)
	getDeckRequest 			--------------------------------->	ok                  (Server sends to the player god, the deck from which he can choose the cards (skimmed according to the lobby size))
    DeckFull				<---------------------------------	Response
	setPickedCardsResponse	--------------------------------->	ok					(Set Deck, Then send choose card, SelectedPlayer is the last)
	
(For players)
	ok						<---------------------------------	setPlayerCard   	(Player chose 1 card, send deck(little))
	setPlayerCardResponse	--------------------------------->	ok					(Server add Player in match, Add card to forbidden cards, Add Color to Player and set PlayersID)
	getWorkersID			--------------------------------->	ok
    WorkersID				<---------------------------------	Response
(For Workers)
	getBattlefield			--------------------------------->	ok
	Battlefield				<---------------------------------	Response
	setInitialWorkerPosition--------------------------------->	ok
(end)
	Battlefield				<---------------------------------	setBattlefield		(notify all)
	Players					<---------------------------------	setPlayers			(notify all (nick+color+card))
(end)

-------------------------------------------------------------------------------------- START MATCH
(For Players)
	ok						<---------------------------------	yourTurn
	startTurn				--------------------------------->	ok
(For Steps)
	ok	(check)				<---------------------------------	playStep
(Do)
	selectWorker	(check)	--------------------------------->	ok
	getWorkerView           --------------------------------->	ok
	WorkerView				<---------------------------------	Response (!!!WorkerView NULL no step)
(While WorkerView NULL)
	skipStepRequest(opt)	--------------------------------->	ok
	playStepRequest(opt)	--------------------------------->	ok
	Battlefield				<---------------------------------	setBattlefield		(notify all)
(end)


