	CLIENT														SERVER

-------------------------------------------------------------------------------------- MATCH CREATION
(Do)
	addPlayerRequest		--------------------------------->	ok					{"action":"addPlayer","data":{"playerNickname":"Steve Jobs","lobby":2 }}
	Added/NickNameExist		<---------------------------------	Response			(Server check nick presence in every lobby)
(While nick invalid)
	
	LobbyReady				<---------------------------------	lobbyReady			(Server start match when a Lobby is Full, notify all client in Lobby by nick)
	ok						<---------------------------------	getChooseCards		(SelectedPlayer, choose 3 or 2 cards)
	getChooseCards			--------------------------------->	ok					(Set Deck, Then send choose card, SelectedPlayer is the last)
	
(For players)
	ok						<---------------------------------	playerChooseCard	(Player notify by server chose card, send forbidden cardsName, add color serverSide)
	playerChooseCard		--------------------------------->	ok					(Server add Player in match, Add card to forbidden cards)
(For Workers)
	getBattlefield			--------------------------------->	ok
	Battlefield				<---------------------------------	Response
	setInitialWorkerPosition--------------------------------->	ok
(end)
	getWorkersID			--------------------------------->	ok
	WorkersID				<---------------------------------	Response
	Battlefield				<---------------------------------	setBattlefield		(notify all (nick+color+card))
	Players					<---------------------------------	setPlayers			(notify all)
(end)

-------------------------------------------------------------------------------------- START MATCH
(For Players)
	ok						<---------------------------------	yourTurn
	startTurn				--------------------------------->	ok
(For Steps)
	ok	(check)				<---------------------------------	playStep
(Do)
	selectWorker	(check)	--------------------------------->	ok
	WorkerView				<---------------------------------	setWorkerView (!!!WorkerView NULL no step)
(While WorkerView NULL)
	skipStepRequest(opt)	--------------------------------->	ok
	playStepRequest(opt)	--------------------------------->	ok
	Battlefield				<---------------------------------	setBattlefield		(notify all)
(end)


